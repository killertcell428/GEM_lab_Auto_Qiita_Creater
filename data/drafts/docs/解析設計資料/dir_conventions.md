# ディレクトリ運用ポリシー参照

共通ポリシーは `/home/uecha/shared/dir_conventions.md` を参照してください。  
本プロジェクトでは大容量データは `/home/uecha/data/` 配下の原本をリンクで参照し、`docs/` `scripts/` `simulation/` `analysis/` `reference/` の設定・スクリプトのみをGit管理します。

